# Keep this folder

Place Vinculum OPDD supporting assets here (images, CSS, fonts).

Reference them from `../index.html` using relative paths like `./assets/diagram.png`.

Everything under `/new-games/` is protected by Cloudflare Access (GitHub members only).
